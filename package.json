{
  "name": "bun-react-template",
  "version": "0.1.0",
  "homepage": "https://teasec4.github.io/quiz-app-react-bun-ts",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --hot src/index.ts",
    "build": "bun build ./src/index.html --outdir=dist --sourcemap --target=browser --minify --define:process.env.NODE_ENV='\"production\"' --env='BUN_PUBLIC_*' --define:process.env.PUBLIC_URL='/quiz-app-react-bun-ts'",
    "start": "NODE_ENV=production bun src/index.ts",
    "predeploy": "bun run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "bun-plugin-tailwind": "^0.1.2",
    "gh-pages": "^6.3.0",
    "react": "^19",
    "react-dom": "^19",
    "react-icons": "^5.5.0",
    "react-router": "^7.13.1",
    "tailwindcss": "^4.2.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/react": "^19",
    "@types/react-dom": "^19"
  }
}
